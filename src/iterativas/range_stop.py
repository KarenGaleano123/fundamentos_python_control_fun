# Se utiliza un bucle 'for' junto con la función 'range()'
# 'range(stop)' genera una secuencia de números desde 0 hasta stop-1

for i in range(stop):
    # En cada iteración, 'i' toma un valor de la secuencia
    # Código a ejecutar en cada vuelta del ciclo
    print(i)